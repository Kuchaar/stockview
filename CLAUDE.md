# CLAUDE.md
@AGENTS.md

## Claude Code
- Session commands: /sv-start (sync + where we left off), /sv-koniec (build, journal entry, commit, push).
- Shared settings: .claude/settings.json (committed). Personal: .claude/settings.local.json and CLAUDE.local.md (gitignored).
